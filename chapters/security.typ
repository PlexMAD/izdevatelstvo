#import "../styles.typ": myenum

== Информационная безопасность

#set par(justify: true, leading: 1.25em, first-line-indent: (amount: 1.25em, all: true))

Вопросы информационной безопасности имеют ключевое значение для веб-приложений, обрабатывающих пользовательские учётные записи и загружаемый контент. В системе ConSuccess реализован набор мер, направленных на защиту учётных данных пользователей, предотвращение несанкционированного доступа к защищённым операциям и противодействие распространённым уязвимостям веб-приложений. В данном разделе описаны принятые проектные решения и особенности их реализации.

=== Хранение паролей

Пароли пользователей никогда не сохраняются в базе данных в открытом виде. При регистрации пользователя пароль хешируется алгоритмом bcrypt с коэффициентом десяти раундов соления. Функции хеширования и сравнения вынесены в модуль `utils/crypt.ts` серверной части. При входе в систему введённый пароль сравнивается с сохранённым хешем через функцию bcrypt, что исключает возможность восстановления исходного пароля даже при утечке базы данных. Выбор bcrypt обусловлен его устойчивостью к перебору: алгоритм намеренно спроектирован медленным и адаптивным, что замедляет атаки перебором паролей и атаки по словарю.

=== Авторизация через JWT

Авторизация пользователей реализована на базе JSON Web Token (JWT). При успешной проверке учётных данных `AuthService` формирует токен, подписанный секретным ключом `JWT_SECRET`, хранящимся в переменных окружения. Полезная нагрузка токена содержит идентификатор пользователя, имя пользователя и роль, что позволяет серверу восстанавливать контекст пользователя без обращения к базе данных при каждом запросе.

Защищённые эндпоинты используют `AuthGuard`, который выполняет следующие действия:
#myenum[
  Извлекает Bearer-токен из заголовка `Authorization`.
][
  Проверяет подпись токена через `JwtService.verifyAsync` с использованием секрета из `ConfigService`.
][
  При успехе прикрепляет раскодированную полезную нагрузку к объекту запроса (`request.user`), делая данные пользователя доступными в контроллерах.
][
  При отсутствии или недействительности токена возбуждает `UnauthorizedException`, что приводит к ответу со статусом 401.
]

Подпись токена защищает его от подделки: любой изменённый токен не пройдёт проверку подписи. Срок действия токена задаётся при его генерации и ограничивает время, в течение которого украденный токен может быть использован.

=== HTTP-only cookies

Для хранения JWT-токена на стороне клиента используется HTTP-only cookie с именем `access_token`. Токен никогда не передаётся в JavaScript-код браузера: cookie устанавливается серверным Next.js API-маршрутом `POST /api/auth/login` после успешного получения токена от бэкенда NestJS. При последующих запросах клиентский код Next.js не работает с токеном напрямую — вместо этого все обращения к бэкенду идут через серверные маршруты `app/api/`, которые читают cookie из запроса и добавляют заголовок `Authorization: Bearer ...` перед перенаправлением запроса на бэкенд.

Такой подход обеспечивает защиту от нескольких типов атак. Во-первых, злоумышленник не может украсть токен через XSS-уязвимость: даже при внедрении произвольного JavaScript-кода на страницу он не сможет прочитать HTTP-only cookie. Во-вторых, клиентский код не содержит токен в DOM, переменных или localStorage, что уменьшает поверхность атаки.

=== Ролевая модель доступа

В системе реализована ролевая модель с тремя уровнями доступа: `USER`, `MODERATOR` и `ADMIN`. Роль сохраняется в поле `role` таблицы `User` и передаётся в составе JWT-токена. Проверка роли выполняется в контроллерах при выполнении привилегированных операций:

#myenum[
  Обычный пользователь может создавать публикации, редактировать и удалять собственные посты, добавлять посты в избранное и убирать их оттуда.
][
  Модератор дополнительно имеет право удалять любые публикации для поддержания качества контента.
][
  Администратор обладает полными правами: помимо возможностей модератора он может добавлять университеты и дисциплины в каталог.
]

Клиентская часть также учитывает роль пользователя при отображении интерфейса: кнопки «Добавить» на страницах каталога показываются только администраторам, а кнопки «Редактировать» и «Удалить» на странице поста — только автору публикации или пользователю с ролью модератора/администратора. Однако клиентские проверки носят вспомогательный характер: основная проверка прав выполняется на сервере, что исключает возможность обхода ограничений путём прямого обращения к API.

=== Валидация входных данных

Все входные данные, поступающие от пользователя, проходят валидацию на сервере. Для полей формы проверяется наличие обязательных значений и соответствие типам; при нарушениях возбуждается `BadRequestException` со статусом 400. Для загружаемых файлов применяются дополнительные ограничения:

#myenum[
  Проверка MIME-типа: принимаются только файлы, начинающиеся с `image/`. Файлы с другими типами отклоняются на уровне `fileFilter` библиотеки multer.
][
  Ограничение максимального размера файла и количества файлов в одном запросе (до пяти изображений для публикации).
][
  Генерация уникальных имён файлов по шаблону `{timestamp}-{random}.{ext}`, что исключает коллизии и возможность перезаписи чужих файлов.
]

Такая валидация предотвращает загрузку вредоносных файлов (например, скриптов с расширением `.php` или `.js`) под видом изображений и защищает файловую систему сервера от переполнения.

=== Защита от SQL-инъекций

Все запросы к базе данных формируются через Prisma Client, который использует параметризованные запросы. Значения полей передаются в виде аргументов запроса, а не конкатенируются в строку SQL, что исключает возможность внедрения постороннего SQL-кода через пользовательский ввод. Декларативная схема базы данных и типобезопасный клиент обеспечивают дополнительный уровень защиты: невозможно сформировать некорректный запрос, используя поля, отсутствующие в модели.

=== Защита от XSS

Поскольку React по умолчанию экранирует все строковые значения при рендеринге, базовая защита от XSS-атак обеспечивается на уровне фреймворка. Пользовательский текст (заголовки и тела публикаций) выводится в компонентах без использования `dangerouslySetInnerHTML`, что исключает интерпретацию HTML-кода из пользовательского ввода. Дополнительную защиту обеспечивает использование HTTP-only cookies для хранения токена, как описано выше.

=== Настройка CORS

Для ограничения возможных источников запросов к бэкенду NestJS в приложении настроен CORS: разрешены только запросы с домена фронтенда (`http://localhost:3000` в среде разработки). Это предотвращает выполнение запросов к API из сторонних доменов и защищает пользователей от атак типа CSRF в сочетании с использованием HTTP-only cookies.

Таким образом, в системе ConSuccess реализован комплекс мер информационной безопасности, охватывающих аутентификацию, авторизацию, валидацию входных данных, защиту от распространённых уязвимостей веб-приложений и ограничение поверхности атаки. Такой подход обеспечивает защиту как пользовательских данных, так и внутренней инфраструктуры системы.
