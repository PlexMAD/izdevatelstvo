#import "../styles.typ": myenum

== Выбор технологического стека

#set par(justify: true, leading: 1.25em, first-line-indent: (amount: 1.25em, all: true))

Выбор технологий для разработки веб-приложения ConSuccess осуществлялся исходя из требований к системе, необходимости поддержки серверного рендеринга, удобства работы с базой данных и загрузки файлов, а также возможности обеспечить безопасную авторизацию. Стек разделён на серверную и клиентскую части.

=== Серверная часть

В качестве серверного фреймворка выбран *NestJS* — фреймворк для Node.js на базе TypeScript. NestJS реализует модульную архитектуру с внедрением зависимостей, предоставляет удобные декораторы для описания маршрутов (`@Controller`, `@Get`, `@Post`), защитников маршрутов (`@UseGuards`) и перехватчиков файлов (`@UseInterceptors`). Это позволяет структурировать код по доменным модулям (пользователи, университеты, дисциплины, посты, города) без избыточного дублирования.

Для работы с базой данных применяется *Prisma ORM* — типобезопасный инструмент доступа к данным, обеспечивающий автоматическую генерацию клиента на основе декларативной схемы (`schema.prisma`). Prisma поддерживает миграции, что упрощает развитие структуры базы данных в процессе разработки. В качестве СУБД используется *PostgreSQL* — реляционная база данных с поддержкой составных первичных ключей (применяется в таблице избранного `Favorite`), уникальных ограничений и внешних ключей.

Аутентификация реализована на основе *JWT (JSON Web Token)*. При успешном входе сервер формирует токен, подписанный секретным ключом (`JWT_SECRET`), и возвращает его клиенту. Клиентская сторона хранит токен в HTTP-only cookie, что исключает доступ к нему из JavaScript и снижает риск XSS-атак. Защищённые эндпоинты проверяют токен через `AuthGuard`, который извлекает Bearer-токен из заголовка `Authorization` и верифицирует его с помощью `JwtService`.

Загрузка изображений обеспечивается библиотекой *Multer*, интегрированной в NestJS через `FilesInterceptor`. Файлы сохраняются на диск в папку `uploads/` с именем вида `{timestamp}-{random}.{ext}`. Принимаются только файлы с MIME-типом `image/*`; URL сохранённого файла записывается в таблицу `Attachment`. Папка `uploads/` отдаётся клиентам как статические ресурсы по пути `/uploads`.

=== Клиентская часть

Клиентская часть разработана на основе *Next.js* (App Router, React 19). Next.js поддерживает серверные компоненты (Server Components), которые получают данные напрямую на сервере и передают их клиенту в виде готового HTML — это ускоряет первоначальную загрузку и упрощает работу с cookie на сервере. API-маршруты Next.js (`app/api/`) используются как прокси между клиентом и бэкендом NestJS: они читают cookie `access_token`, добавляют его в заголовок `Authorization: Bearer` и перенаправляют запрос на бэкенд.

Для стилизации интерфейса применяется *Tailwind CSS v4* совместно с компонентной библиотекой *shadcn/ui* (стиль new-york, нейтральная цветовая схема). Это обеспечивает единообразный внешний вид всех страниц при минимальном объёме пользовательских стилей. HTTP-запросы к бэкенду выполняются через *Axios* с заранее сконфигурированным экземпляром (`baseURL: http://127.0.0.1:5000`).

Управление состоянием форм реализовано с помощью *React Hook Form*: поля проходят клиентскую валидацию, ошибки отображаются под каждым полем, серверные ошибки — через `setError("root")`. Для кэширования серверных данных и управления повторными запросами используется *TanStack Query v4*.

#pagebreak()

== Проектирование модели данных

#set par(justify: true, leading: 1.25em, first-line-indent: (amount: 1.25em, all: true))

Модель данных системы ConSuccess спроектирована в виде реляционной схемы и описана с помощью Prisma Schema. Схема содержит семь сущностей, связанных отношениями «один ко многим» и «многие ко многим» (через таблицу-связку).

*User* — пользователь системы. Хранит уникальное имя пользователя (`username`), хеш пароля и роль (`USER`, `MODERATOR`, `ADMIN`). Роль определяет права: обычный пользователь создаёт и редактирует собственные публикации; модератор и администратор могут удалять любые публикации; администратор дополнительно управляет каталогом (добавляет университеты и дисциплины).

*City* — город. Содержит уникальное название. Используется при создании университета для указания местоположения.

*University* — университет. Содержит название, ссылку на город (`cityId`) и необязательный путь к логотипу (`avatar`). Является корневым уровнем иерархии каталога.

*Subject* — учебная дисциплина. Привязана к конкретному университету через внешний ключ `universityId`. Образует второй уровень иерархии каталога.

*Post* — публикация. Содержит заголовок (`title`), основной текст (`body`), дату создания (`createdAt`), идентификатор автора (`userId`) и необязательный идентификатор дисциплины (`subjectId`). Если `subjectId` равен `null`, публикация относится к разделу «Знания» и не привязана к конкретному университету или дисциплине.

*Attachment* — вложение (изображение). Хранит относительный URL файла на сервере (вида `/uploads/имя-файла.расширение`) и идентификатор поста (`postId`). Один пост может иметь до пяти вложений.

*Favorite* — избранное. Таблица-связка между пользователем и публикацией. Имеет составной первичный ключ `[userId, postId]`, что исключает дублирование записей при повторном добавлении одного и того же поста в избранное.

#par(first-line-indent: 0pt)[Таблица 5 – Сущности модели данных]
#align(center)[#set par(leading: 0.5em)
  #table(
    columns: (1fr, 1fr, 1.5fr),
    align: (left, left, left),
    [*Сущность*], [*Ключевые поля*], [*Связи*],

    [User],
    [id, username (unique), passwordHash, role],
    [→ Post (один ко многим), → Favorite (один ко многим)],

    [City],
    [id, name (unique)],
    [→ University (один ко многим)],

    [University],
    [id, name, cityId, avatar?],
    [← City, → Subject (один ко многим)],

    [Subject],
    [id, name, universityId],
    [← University, → Post (один ко многим)],

    [Post],
    [id, title, body, subjectId?, userId, createdAt],
    [← Subject (необязательно), ← User, → Attachment, → Favorite],
  )
]

#pagebreak()

#par(first-line-indent: 0pt)[Продолжение Таблицы 5]
#align(center)[#set par(leading: 0.5em)
  #table(
    columns: (1fr, 1fr, 1.5fr),
    align: (left, left, left),
    [*Сущность*], [*Ключевые поля*], [*Связи*],

    [Attachment],
    [id, url, postId],
    [← Post],

    [Favorite],
    [userId + postId (составной PK)],
    [← User, ← Post],
  )
]